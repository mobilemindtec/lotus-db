%% -*- erlang -*-
{application, lotus_db,
  [{description, "Rest framework built on top of sql_bridge"},
   {vsn, "0.1"},
   {modules, [lotus_db,
              lotus_db_util,
              lotus_db_uuid]},
   {registered, []},
   {included_applications, [mysql, poolboy, sql_bridge, erlias]},
   %{mod, {'lotus_db_app', []}},
   {env, []},
   {applications, [kernel, stdlib, crypto]}]}.